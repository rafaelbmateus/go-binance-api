version: "3.9"

services:
  app:
    build:
      context: ../
      dockerfile: build/Dockerfile
    restart: on-failure
    env_file:
      - ./../.env
    environment:
      - HOST=0.0.0.0:3000
      - CGO_ENABLED=0
      - GOOS=linux
    ports:
      - "3000:3000"
    command: go run main.go
